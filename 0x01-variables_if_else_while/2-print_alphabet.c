#include <stdio.h>

/**
 * main - char function
 * Return: 0 (Success)
 * Description: Call the alphabet game
 */

int main(void)/** must retun void or zero*/
{
	char a;/**Assign a value a */

	a = 'a';
	while
		(a <= 'z')
		{ putchar (a);
			a++;
		}
	putchar('\n');
	return (0);
}
