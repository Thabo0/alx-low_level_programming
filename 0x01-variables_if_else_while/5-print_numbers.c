#include <stdio.h>

/**
 * main - char function
 * Return: 0 (Success)
 * Description: Print numbers
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
